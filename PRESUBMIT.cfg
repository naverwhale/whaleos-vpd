[Hook Scripts]
cros format = cros format --check --commit ${PRESUBMIT_COMMIT} --include "*.gn" --include "*.md" --include "*.cc" --include "*.h" --exclude "*" ${PRESUBMIT_FILES}
cros lint = cros lint --commit ${PRESUBMIT_COMMIT} ${PRESUBMIT_FILES}

[Hook Overrides]
cros_license_check: false
